> [Вернуться к содержанию документации](./README.md)

# Техническая спецификация Frontend (Next.js)

> **Детали текущей реализации**: Подробный обзор реализованных функций,
> инструкции по запуску и планы дальнейшего развития можно найти в файле
> **[README фронтенда Next.js](../../frontends/nextjs/README_RU.md)**.
>
> **Статус реализации**: 🚧 **В АКТИВНОЙ РАЗРАБОТКЕ** - Фронтенд находится в активной разработке.

## 1. Общие цели
- Разработать **современный, адаптивный frontend для новостного портала**.
- Следовать **принципам дизайна, вдохновленным Google News**: чистый, просторный, mobile-first,
  с высокой читаемостью.
- Обеспечить **дружественную к SEO структуру** со статическими URL для всех новостных статей.
- Гарантировать **доступность (WCAG 2.1 AA)** и **быструю скорость загрузки**.

---

## 2. Технологический стек
- **Фреймворк:** Next.js (React, поддерживает SSR/SSG для SEO и производительности).
- **UI-библиотека:** Material UI (MUI).
- **Стилизация:** Кастомная тема Material UI (переопределения для брендинга, типографики и цветов).
- **Типографика:** Roboto (базовый) + Roboto Slab / Serif для заголовков статей.

---

## 3. Макет и страницы

### 3.1 Главная страница
- Чистая многоколоночная сеточная верстка:
  - Десктоп: 2–3 колонки.
  - Мобильные: 1 колонка.
- Блок с главной статьей наверху (большое изображение, заголовок, анонс).
- Второстепенные статьи ниже с изображениями среднего размера.
- Небольшие новостные заметки в виде компактных карточек.
- Панель навигации по категориям наверху.
- **Примечание**: В дополнение к этому, реализована поддержка динамической компоновки (`Simple` и `Custom` режимы), управляемой из админ-панели.

### 3.2 Страница категории
- Список статей с пагинацией, принадлежащих к выбранной категории.
- Заголовок категории, выделенный акцентным цветом бренда.

### 3.3 Страница статьи
- Крупный заголовок с засечками (serif).
- Главное изображение (адаптивное, до ~800px в ширину).
- Дата публикации, автор и категория.
- Полное содержимое с изображениями и встроенными HTML-элементами.
- Похожие статьи внизу.
- Поддержка SEO-метаданных, OpenGraph и Twitter-карт.

### 3.4 Статические страницы
- Страница "О нас".
- Страница архива (фильтр по дате/категории).

---

## 4. Функциональность

### 4.1 SEO и URL
- **Гибридная стратегия рендеринга**: Использовать возможности рендеринга Next.js для максимизации
  производительности и SEO.
  - **Рендеринг на стороне сервера (SSR)**: Для динамических страниц, таких как главная и списки
    категорий, чтобы контент всегда был свежим и немедленно индексируемым.
  - **Генерация статического сайта (SSG)**: Для отдельных страниц статей и статичных страниц
    (`О нас`, `Контакты`), обеспечивая максимально быструю загрузку.
  - **Инкрементальная статическая регенерация (ISR)**: Опционально использовать ISR для пересборки
    статических страниц в фоновом режиме через заданные интервалы, сочетая скорость статики со
    свежестью динамического контента.
- **Статические URL**: Статические, человекочитаемые URL для статей (формат `/news/{slug}-{id}`,
  например, `/news/zagolovok-stati-15378`) и категорий (`/category/{slug}-{id}`).
- **Структурированные данные**: Внедрить JSON-LD (схема `NewsArticle`) для предоставления богатых
  метаданных поисковым системам, улучшая внешний вид результатов поиска (расширенные сниппеты).
- **Мета-теги**: Динамически генерировать `<title>`, `<meta name="description">` и OpenGraph теги для
  каждой страницы, чтобы обеспечить оптимальное отображение при репостах в социальных сетях и
  корректную индексацию.

### 4.2 Адаптивный дизайн
- Макет по принципу Mobile-first.
- Адаптивная типографика и "резиновые" изображения.
- Рекомендуемые размеры изображений:
  - Главная статья: ~600–800px в ширину.
  - Стандартные статьи: ~320–400px в ширину.
  - Миниатюры: ~160–200px в ширину.

### 4.3 Доступность
- Соответствие WCAG 2.1 AA.
- ARIA-атрибуты, правильный контраст, навигация с клавиатуры.

### 4.4 Темизация и брендинг
- **Базовые цвета**:
  - Основной: Темно-синий (#1c355e).
  - Второстепенный: Глубокий красный (#cc0000).
  - Фон: Белый (#ffffff) или почти белый (#fdfcf8).
  - Нейтральные серые акценты для границ и разделителей (#eeeeee).
- **Использование**:
  - Заголовки и ссылки: Темно-синий.
  - Выделение категорий (например, "Аналитика"): Красный.
  - Панель навигации: Темно-синий фон с белым текстом.
- **Типографика**:
  - Заголовки: С засечками (Roboto Slab).
  - Основной текст: Без засечек (Roboto).

---

## 5. Интеграция с Backend API

### 5.1 Конфигурация API
- **Base URL**: `http://phoebe-app:8080/api` (для Docker) или `http://localhost:8080/api` (локально)
- **Публичные эндпоинты**: `/api/public/*` - доступны без аутентификации
- **Admin эндпоинты**: `/api/admin/*` - требуют Basic Auth
- **CORS**: Настроен для localhost:3000, localhost:3001 и Docker контейнера

### 5.2 Аутентификация
- **Метод**: Basic Authentication с username и password
- **Хранение токена**: localStorage (base64 encoded credentials)
- **Эндпоинт проверки**: `/api/admin/auth/me` - получение данных текущего пользователя
- **Автоматическая обработка ошибок**: Перенаправление на login при 401/403

### 5.3 Структура запросов

**Публичные запросы** (без аутентификации):
```javascript
GET /api/public/news?page=0&size=10
GET /api/public/news/{id}
GET /api/public/news/term/{termId}
GET /api/public/news/search?q=query
GET /api/public/homepage
GET /api/public/homepage/mode
PATCH /api/public/homepage/mode
```

**Admin запросы** (с Basic Auth header):
```javascript
GET /api/admin/auth/me
GET /api/admin/news?page=0&size=20
POST /api/admin/news
PUT /api/admin/news/{id}
DELETE /api/admin/news/{id}
GET /api/admin/terms
POST /api/admin/terms
GET /api/admin/homepage-blocks
POST /api/admin/homepage-blocks
PUT /api/admin/homepage-blocks/{id}
DELETE /api/admin/homepage-blocks/{id}
```

### 5.4 Обработка ошибок
- **401 Unauthorized**: Автоматический logout и редирект на `/admin/login`
- **403 Forbidden**: Автоматический logout и редирект на `/admin/login`
- **Network errors**: Отображение сообщения об ошибке пользователю

---

## 6. Реализованные функции

### 6.1 Основная инфраструктура
- **Настройка проекта**: Next.js с React, Material UI (MUI) и Axios.
- **Docker интеграция**: Dockerfile для Next.js приложения и обновленный docker-compose.yml
  с сервисом nextjs-app на порту 3000.
- **Переменные окружения**: Конфигурация для базового URL бэкенд API (.env.local).

### 6.2 Пользовательский интерфейс и темизация
- **Кастомная тема Material UI**: Поддержка светлой и темной темы с переключателем в футере.
- **Переключатель режимов главной страницы**: В футер добавлена кнопка для смены режима отображения главной страницы (`Simple`/`Custom`).
- **Глобальный Layout компонент**: Единообразный header, основная область контента и footer.
- **Кастомная страница 404**: Для необработанных маршрутов.

### 6.3 Аутентификация и авторизация

**Вход в систему:**
- **URL входа**: `/admin/login` (http://localhost:3000/admin/login)
- **Учетные данные по умолчанию**: username: `admin`, password: `admin`
- **Валидация**: Проверка на фронтенде (username: 3-50 символов, password: минимум 8 символов)

**Управление аутентификацией:**
- **AuthContext**: Глобальное управление статусом аутентификации и ролями (ADMIN/EDITOR)
- **Хранение сессии**: localStorage с base64 encoded credentials
- **Автоматическая проверка**: При загрузке страницы проверяется наличие токена
- **Автоматический logout**: При ошибках 401/403 с редиректом на `/admin/login`

**Защита маршрутов:**
- **ProtectedRoute**: Проверяет аутентификацию, редиректит на `/admin/login` если не авторизован
- **AdminRoute**: Дополнительно проверяет роль ADMIN для административных функций
- **Spinner загрузки**: Отображается во время проверки статуса аутентификации

**AdminBar (черная панель администратора):**
- **Отображение**: Только для аутентифицированных пользователей (заменяет обычный header)
- **Цвет**: Черный фон (#000) для визуального отличия от публичного интерфейса
- **Информация о пользователе**: Отображает имя пользователя и роль (цветной чип)
  - 🔴 Красный чип (#cc0000) для роли ADMIN
  - 🔵 Темно-синий чип (#1c355e) для роли EDITOR

**Ссылки AdminBar (доступны всем авторизованным):**
- Home - возврат на главную страницу
- Create News - создание новой статьи (`/admin/news/new`)
- News List - список всех статей (`/admin/news`)

**Ссылки AdminBar (только для ADMIN):**
- Taxonomy - управление категориями и тегами (`/admin/taxonomy`)
- Users - управление пользователями и ролями (`/admin/users`)
- Homepage - управление блоками главной страницы (`/admin/homepage-blocks`)

**Кнопка Logout**: Выход из системы с редиректом на главную страницу

### 6.4 Публичные страницы контента
- **Главная страница** (`/`): SSR отображение контента с поддержкой двух режимов: `Simple` (список новостей) и `Custom` (блочная структура, управляемая из админ-панели).
- **Страница статьи** (`/node/[id]`): SSG с ISR для оптимальной производительности и SEO
- **Страница категории** (`/category/[id]`): SSR список статей определенной категории
- **Страница поиска** (`/search`): Форма поиска с интеграцией с бэкенд API
- **Технические страницы** (`/page/[slug]`): Динамические страницы из БД (About, Contact, Privacy и т.д.)
- **Страница 404**: Кастомная страница для несуществующих маршрутов

### 6.5 Административная панель (CRUD операции)
- **Управление новостями**: Список, создание, редактирование и удаление статей.
- **Управление таксономией**: Полный CRUD для терминов и категорий.
- **Управление пользователями**: Просмотр и редактирование ролей пользователей (только для ADMIN).
- **Управление главной страницей**: Реализован полный CRUD-интерфейс для создания, редактирования и удаления блоков главной страницы. Форма адаптируется под тип блока (`Новостной` или `Рекламный`).
- **Валидация на фронтенде**: Комплексная валидация форм согласно VALIDATION_GUIDE.md.
- **Универсальная система отката**: Все формы сохранения поддерживают 5-секундное окно для отмены операции с возвратом к редактированию.
  - **Реализация**: Универсальный хук `useUndoSave` для всех CRUD операций
  - **UX**: Snackbar уведомление с кнопкой "UNDO", блокировка интерфейса во время ожидания
  - **Функциональность**: Сохранение состояния формы, возможность отката, восстановление данных редактирования

### 6.6 Обработка контента
- **HTML рендеринг**: Поддержка HTML контента (только разрешенные теги) и YouTube встраиваний в полях teaser и body.
- **Безопасная обработка**: Использование SafeHtml обработки бэкенда.

---

## 7. Будущие улучшения (опционально)
- Полнотекстовый поиск с автодополнением (например, с использованием Pagefind).
- "Ленивая" загрузка изображений и бесконечная прокрутка на страницах категорий.
- Push-уведомления для срочных новостей.
- Интеграция OAuth 2.0 + JWT для улучшенной безопасности аутентификации.
- **Возможности загрузки файлов для изображений и других медиа**: Это напрямую связано с безопасным
  встраиванием медиа. Реализация этой функциональности потребует обновления бэкенда для безопасной
  обработки и хранения загруженных файлов, а также обновления правил санитизации HTML.

---

## Запуск фронтенда Next.js локально

... (остальная часть файла без изменений)
