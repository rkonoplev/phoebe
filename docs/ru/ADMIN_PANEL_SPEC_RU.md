> [Вернуться к содержанию документации](./README.md)

# Техническая спецификация для панели администратора

> Для определения ключевых терминов и технологий, пожалуйста, обратитесь к **[Глоссарию](./GLOSSARY_RU.md)**.
>
> **[Подробное описание реализации фронтенда Next.js](../../frontends/nextjs/README_RU.md)**

Этот документ описывает технические требования к интерфейсу панели администратора Phoebe CMS.

---

## 1. Роли и разрешения

### 1.1 Управление доступом на основе ролей

Phoebe CMS использует двухролевую систему: **ADMIN** (полный доступ) и **EDITOR** (только собственный контент).

**Детали реализации**: См. [Руководство по безопасности ролей](./SECURITY_ROLES_RU.md) для полных требований.

### 1.2 Безопасность в UI
- **Интерфейс на основе ролей**: Элементы UI отображаются/скрываются в зависимости от роли и владения
  контентом.
- **Валидация в реальном времени**: Бэкенд предотвращает неавторизованные операции.

---

## 2. Управление контентом

### 2.1 Страница со списком контента

#### Для пользователей с ролью ADMIN
- **Список с пагинацией** всех элементов контента.
- **Полный набор кнопок действий** для каждого элемента:
  - **Создать**: Переход к форме создания.
  - **Редактировать**: Переход к форме редактирования (любой элемент).
  - **Удалить**: Удаление любого элемента с подтверждением.
  - **Опубликовать/Снять с публикации**: Изменение статуса любого элемента.
- **Массовые операции**:
  - Чекбоксы для выбора нескольких статей.
  - Кнопка "Select All" для выбора всех статей на странице.
  - Кнопка "Delete Selected" для массового удаления.
  - Кнопка "Unpublish Selected" для массового снятия с публикации.
- **Визуальные индикаторы**:
  - Цветные чипы статуса (Published/Draft).
  - Колонка с автором статьи.
  - Сообщения об успешном выполнении операций.

#### Для пользователей с ролью EDITOR
- **Список с пагинацией** всех элементов (чужой контент в режиме только для чтения).
- **Ограниченный набор кнопок** в зависимости от авторства:
  - **Создать**: Переход к форме создания.
  - **Редактировать**: Доступно только для собственных материалов.
  - **Удалить**: Доступно только для собственных материалов, с подтверждением.
  - **Опубликовать/Снять с публикации**: Доступно только для собственных материалов.

---

## 3. Термины таксономии

### 3.1 Страница управления терминами (только ADMIN)
- **Список с пагинацией** терминов таксономии.
- **Колонка ID** для идентификации терминов.
- Функциональность **добавления новых терминов**.
- **Редактирование существующих терминов**.
- **Удаление существующих терминов** с подтверждением.
- Управление словарями (например, категории, теги, page_type).
- **Цветные чипы** для визуального различия ролей.

---

## 4. Массовые операции

### 4.1 Массовые операции для ADMIN
- **Массовое удаление/снятие с публикации** выбранного контента.
- **Чекбоксы для выбора**: Индивидуальный выбор статей.
- **Select All**: Выбор всех статей на текущей странице.
- **Массовое управление ролями** пользователей.
- **Общесистемные операции** с диалогами подтверждения.
- **Фильтры для массовых операций**:
  - BY_IDS: Операции с выбранными ID.
  - BY_TERM: Выбор контента по термину.
  - BY_AUTHOR: Выбор контента по автору.
  - ALL: Операции со всем контентом.
- **Визуальная обратная связь**: Сообщения об успешном выполнении операций.

### 4.2 Массовые операции для EDITOR (ЗАПРЕЩЕНЫ)
- **НЕТ МАССОВЫХ ОПЕРАЦИЙ** - роль EDITOR ограничена операциями с одним элементом.
- **Удаление**: Может удалять только один элемент за раз (собственный контент).
- **Публикация**: Может изменять статус только одного элемента за раз (собственный контент).
- **Безопасность**: Бэкенд запрещает EDITOR доступ к эндпоинтам массовых операций.

---

## 5. Страница создания/редактирования контента

### 5.1 Поля формы
- **Заголовок**: Текстовое поле с валидацией (5-50 символов).
- **Анонс**: Текстовая область для превью (10-250 символов).
- **Тело контента**: Текстовая область с поддержкой HTML (минимум 20 символов).
- **Выбор термина**: Выпадающее меню для назначения термина.
- **Статус публикации**: Переключатель Published/Draft.
- **Дата публикации**: Назначается автоматически при создании, но может быть изменена. Формат даты должен соответствовать ISO-8601 (например, `2024-01-15T10:30:00`).

### 5.2 Функция Undo (отмена сохранения)
- **5-секундная задержка** перед фактическим сохранением.
- **Snackbar уведомление** внизу экрана с текстом "Saving in 5 seconds...".
- **Кнопка UNDO**: Синяя кнопка для отмены сохранения.
- **Блокировка формы**: Кнопки Create/Cancel заблокированы во время ожидания.
- **Автоматическая отмена**: Таймер очищается при размонтировании компонента.
- **Сохранение данных**: Все поля остаются заполненными при отмене.

### 5.3 Поддержка HTML контента
- **HTML теги разрешены**: Поля Teaser и Body поддерживают HTML.
- **Встраивание изображений**: `<img src='...' />` разрешено.
- **Встраивание YouTube**: `<iframe>` для YouTube видео разрешено.
- **Валидация на бэкенде**: Очистка опасных тегов и атрибутов.
- **Подсказки в UI**: Helper text информирует о поддержке HTML.

---

## 6. Безопасность и валидация ввода

### 6.1 Валидация ввода
- **Строгие правила валидации** для всех полей ввода.
- **Предотвращение техник взлома**: SQL-инъекции, XSS.
- **CSRF защита**: На бэкенде отключена для API (стандартная практика для RESTful API). Фронтенд должен быть спроектирован с учетом этого.

### 6.2 Безопасность HTML контента
- **Разрешенные теги**: `<img>`, `<iframe>`, `<a>`, `<p>`, `<br>`, `<strong>`, `<em>`, и другие безопасные теги.
- **Очистка контента**: Удаление опасных атрибутов (onclick, onerror и т.д.).
- **Подход "белого списка"** для разрешенных HTML-тегов и атрибутов.
- **XSS защита**: Валидация и санитизация на бэкенде.

### 6.3 Аутентификация и безопасность
- **Текущая аутентификация**: Spring Security с Basic Auth.
- **Rate Limiting**: 5 попыток входа за 5 минут с одного IP.
- **Защита от brute-force**: Bucket4j для ограничения запросов.
- **HTTP 429**: Сообщение "Too many login attempts. Please wait 5 minutes and try again."
- **Управление паролями**: BCrypt хеширование (сила 12), пароли хранятся в БД.
- **Смена пароля**: Через страницу редактирования пользователя (минимум 8 символов).
- **Доступ администратора**: Контролируется через конфигурацию приложения (например, через миграции Flyway или ручное создание), а не через саморегистрацию пользователей.
- **Планируемая миграция**: OAuth 2.0 + 2FA для всех ролей (ADMIN, EDITOR).

---

## 7. AdminBar (панель администратора)

### 7.1 Внешний вид
- **Цвет**: Черный фон (#000) для визуального отличия.
- **Позиция**: Заменяет обычный header для авторизованных пользователей.
- **Информация о пользователе**:
  - Имя пользователя.
  - Цветной чип роли (🔴 красный #cc0000 для ADMIN, 🔵 синий #1c355e для EDITOR).

### 7.2 Навигация (для всех авторизованных)
- **Home**: Возврат на главную страницу.
- **Create News**: Создание новой статьи.
- **News List**: Список всех статей.

### 7.3 Навигация (только для ADMIN)
- **Taxonomy**: Управление категориями и тегами.
- **Users**: Управление пользователями.
- **Settings**: Настройки сайта.

### 7.4 Кнопка Logout
- **Выход из системы** с редиректом на главную страницу.

---

## 11. Технические требования

### 7.1 Пагинация
- **Все страницы со списками** должны включать пагинацию.
- **Настраиваемые размеры страниц** (10, 25, 50, 100 элементов на страницу) для выбора в UI. Бэкенд поддерживает размер страницы до 100 элементов.
- **Элементы управления навигацией** (Назад, Вперед, номера страниц).

### 7.2 Пользовательский опыт
- **Диалоги подтверждения** для деструктивных действий (удаление, массовые операции).
- **Уведомления об успехе/ошибке** для всех операций.
- **Snackbar уведомления**: Для Undo функции и других временных сообщений.
- **Цветные индикаторы**: Чипы для статусов и ролей.
- **AdminBar**: Черная панель навигации с информацией о пользователе.
- **Адаптивный дизайн** для планшетов и десктопов.
- **Поддержка навигации с клавиатуры** для доступности.

## 8. Управление пользователями (только ADMIN)

### 8.1 Страница списка пользователей
- **Список всех пользователей** с пагинацией.
- **Информация**: Username, Email, Active status, Roles.
- **Цветные чипы ролей**: Красный для ADMIN, синий для EDITOR.
- **Кнопка создания**: Переход к форме создания пользователя.
- **Кнопки действий**: Edit и Delete для каждого пользователя.

### 8.2 Создание/редактирование пользователя
- **Поля формы**:
  - Username (3-50 символов, обязательное).
  - Email (валидация email, обязательное).
  - Password (минимум 8 символов, обязательное при создании).
  - Roles (множественный выбор: ADMIN, EDITOR).
  - Active status (переключатель).
- **Смена пароля**: При редактировании - оставить пустым для сохранения текущего.
- **Валидация**: На фронтенде и бэкенде.

## 9. Настройки сайта (только ADMIN)

### 9.1 Страница настроек
- **Базовая информация**:
  - Site Title.
  - Site URL.
  - Logo URL.
- **SEO настройки**:
  - Meta Description.
  - Meta Keywords.
- **Кастомный HTML**:
  - Header HTML (вставка в `<head>`).
  - Footer HTML (вставка перед `</body>`).
- **Навигационное меню**:
  - Main Menu Term IDs (список ID терминов для главного меню).

## 10. Статические страницы

### 10.1 Архитектура
- **База данных**: Технические страницы хранятся как обычные новости.
- **Vocabulary**: Специальный словарь `page_type` для идентификации.
- **Доступные страницы**: About, Contact, Privacy, Terms, Advertising, Editorial Statute, Community Rules, Technical Sheet.
- **Динамический роутинг**: `/page/[slug]` на фронтенде.
- **SSG + ISR**: Статическая генерация с инкрементальной регенерацией (revalidate: 3600).
