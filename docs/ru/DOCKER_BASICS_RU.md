# Docker для начинающих

> Этот документ предназначен для разработчиков, **которые впервые работают с Docker** или хотят лучше
> понять, что именно происходит при работе с контейнерами в рамках этого проекта.
>
> Если вы уже уверенно используете `make run`, `docker compose` и понимаете разницу между контейнером,
> образом и томом — этот файл можно читать выборочно.

---

## Зачем вообще нужен Docker в этом проекте

Docker позволяет запускать проект **в одинаковой среде у всех разработчиков**, независимо от:

*   версии Java,
*   версии Node.js,
*   операционной системы,
*   установленных локальных зависимостей.

Идея простая:

> *Вы не настраиваете окружение — вы его запускаете.*

В большинстве случаев **единственное, что вам нужно установить локально — это Docker Desktop**.

---

## Базовые понятия (очень важно)

### Образ (Image)

**Docker-образ** — это шаблон.

Можно думать о нём как о:

*   ISO-образе системы,
*   или архиве с уже установленным ПО.

Образ:

*   **не запускается сам по себе**,
*   используется как основа для контейнеров.

Примеры:

*   `mysql:8.0`
*   `openjdk:21-jdk`
*   `phoebe-cms:latest`

---

### Контейнер (Container)

**Контейнер** — это *запущенный экземпляр образа*.

Если образ — это «чертёж», то контейнер — это «работающий процесс».

Контейнер:

*   можно запускать и останавливать,
*   можно удалять и пересоздавать,
*   **не хранит данные надёжно сам по себе**.

При остановке и удалении контейнера:

*   всё, что не вынесено во внешние хранилища, пропадает.

---

### Том (Volume)

**Volume (том)** — это место, где Docker хранит данные **вне контейнера**.

Используется для:

*   базы данных,
*   кэшей сборки,
*   зависимостей.

В этом проекте volumes используются для:

*   `mysql_data` — данные MySQL
*   `gradle_cache` — зависимости Gradle
*   `node_modules` — зависимости фронтенда
*   `nextjs_cache` — кэш Next.js

💡 **Важно:**
Удаление контейнера ≠ удаление данных.
Данные живут в volume, пока вы его явно не удалите.

---

### Docker Compose

**Docker Compose** — это инструмент для запуска **нескольких контейнеров сразу**.

Он читает файл `docker-compose.yml` (или несколько файлов) и:

*   поднимает нужные сервисы,
*   создаёт сети,
*   подключает volumes,
*   задаёт переменные окружения.

В этом проекте используется **несколько compose-файлов**, которые комбинируются.

---

## Как Docker используется в этом проекте

Проект запускается в одном из двух режимов:

### 1. Гибридный режим (для бэкенд-разработки)

*   База данных работает в Docker
*   Бэкенд запускается в контейнере
*   Исходный код **смонтирован внутрь контейнера**
*   Изменения в Java-коде подхватываются автоматически

Требуется:

*   установленный локально JDK (для IDE)

Запуск:

```bash
make run
```

---

### 2. Продакшен-подобный режим

*   Всё работает как в реальном продакшене
*   В контейнере только собранный `.jar`
*   Исходного кода внутри контейнера нет

Используется для:

*   фронтенд-разработки
*   тестирования
*   CI/CD

Запуск:

```bash
make run-prod
```

---

## Основные команды Docker (минимум необходимого)

### Посмотреть запущенные контейнеры

```bash
docker ps
```

### Посмотреть все контейнеры (включая остановленные)

```bash
docker ps -a
```

### Остановить контейнер

```bash
docker stop <container_id>
```

### Удалить контейнер

```bash
docker rm <container_id>
```

---

## Как безопасно «перезапустить всё»

### Рекомендуемый способ (через Makefile)

```bash
make reset
make run
```

⚠️ **Это удалит базу данных проекта.**

---

### Через Docker Compose вручную

```bash
docker compose down --volumes
docker compose up --build
```

---

## Что делать, если «Docker сломался»

### Симптомы

*   контейнеры не стартуют
*   изменения не подхватываются
*   странные ошибки, которых раньше не было

### Решение (в порядке усиления)

1.  Перезапустить проект:

    ```bash
    make stop
    make run
    ```

2.  Принудительно пересоздать контейнеры:

    ```bash
    docker compose -f docker-compose.base.yml -f docker-compose.hybrid.yml up --build --force-recreate
    ```

3.  Полностью очистить Docker (опасно, удаляет данные):

    ```bash
    docker system prune
    docker volume prune
    ```

---

## Частые ошибки новичков

❌ Удалить контейнер и ожидать, что данные пропадут

✔ Данные хранятся в volumes — их нужно удалять отдельно

---

❌ Постоянно выполнять `docker system prune`

✔ Используйте его **только если понимаете последствия**

---

❌ Игнорировать Makefile

✔ Makefile — это безопасная оболочка над Docker-командами

---

## Итог

Если кратко:

*   **Образ** — шаблон
*   **Контейнер** — запущенный процесс
*   **Volume** — данные
*   **Docker Compose** — оркестратор

Если что-то пошло не так — **не паникуйте и не сносите Docker первым делом**.
В большинстве случаев достаточно:

```bash
make reset
make run
```

---

> После освоения этого документа рекомендуется вернуться к
> **[Руководству по Docker](./DOCKER_GUIDE_RU.md)** и использовать его как основной справочник проекта.
